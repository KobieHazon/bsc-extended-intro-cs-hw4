[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bsc-extended-intro-cs-hw4"
version = "1.0.0"
description = "Higher-order functions, dynamic programming, game solving, and random walks"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Kobie Hazon" }]
dependencies = []

[project.scripts]
extended-intro-hw4 = "extended_intro_hw4.cli:cli"

[dependency-groups]
dev = [
    "pytest>=8.0,<9",
    "ruff>=0.12,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/extended_intro_hw4"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = ["assignment"]

[tool.ruff.lint]
select = ["E", "F", "I", "B"]

